import { FC } from 'react';
import {Box} from '@mui/material';


const Landing: FC = () => {
  return (
    <Box
      sx={{
        width: 300,
        margin: 'auto',
        mt: 10,
        display: 'flex',
        flexDirection: 'column',
        gap: 2,
      }}
    >
    </Box>
  );
};

export default Landing;
